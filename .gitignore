# Logs and runtime artifacts
*.log
Thumbs.db
Desktop.ini

# Validation / hardening reports (generated outside normal flow)
validation-*.json
platform-hardening-*.json
**/validation-*.json
**/platform-hardening-*.json

# Local overrides (never commit secrets)
.env
.env.*
*.local.ps1
secrets/
credentials.json

# Windows / editor
.vscode/
*.swp
*~

# Scratch and WIP (do not ship to public users)
*.tmp
*.bak
modules/Shell.ps1.tmp
Test-MenuDeepAudit.ps1

# Optional local theme experiments
terminal/homebase-clean.omp.json

# Generated audit copies (if synced into repo by mistake)
audit-*.json
**/reports/*.json

# Editor / IDE local
.cursor/

# Optional local theme override (machine-specific)
terminal/active-theme.omp.json

# C:\Logs lives outside repo — no action needed
